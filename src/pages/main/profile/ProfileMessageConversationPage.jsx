import { useParams } from 'react-router-dom';

const ProfileMessageConversationPage = () => {
	// eslint-disable-next-line no-unused-vars
	const { conversationId } = useParams();
	return <div>profileMessageConverstation</div>;
};

export default ProfileMessageConversationPage;
