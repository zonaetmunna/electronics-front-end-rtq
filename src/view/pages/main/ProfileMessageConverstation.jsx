import React from "react";
import { useParams } from "react-router-dom";

const ProfileMessageConverstation = () => {
  const { conversationId } = useParams();
  return <div>profileMessageConverstation</div>;
};

export default ProfileMessageConverstation;
